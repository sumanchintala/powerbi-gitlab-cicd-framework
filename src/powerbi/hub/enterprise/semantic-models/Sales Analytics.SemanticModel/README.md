# Sales Analytics.SemanticModel

This folder is a placeholder for a real PBIP semantic model folder.

In an actual implementation, create or open a Power BI Project file in Power BI Desktop and save the semantic model content into this folder.

Example real PBIP semantic model folder contents may include files such as:

```text
Sales Analytics.SemanticModel/
├── definition/
├── .platform
└── item.metadata.json
```

Do not commit real client data or secrets.
